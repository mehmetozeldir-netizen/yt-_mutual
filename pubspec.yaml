name: yt_mutual
description: "YT Mutual - Kanalınız için daha fazla abone, beğeni ve izlenme kazanın."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.2

dev_dependencies:
dependencies:
  flutter:
    sdk: flutter
  google_sign_in: ^6.2.2
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png" # İkon görselinin konumu
  min_sdk_android: 21

flutter:
  uses-material-design: true
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
